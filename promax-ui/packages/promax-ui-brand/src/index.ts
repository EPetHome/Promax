export {
  applyStandaloneTheme,
  installPromaxGlobalTheme,
  PROMAX_GLOBAL_THEME_CSS,
  PROMAX_THEME_TOKENS,
} from './theme.ts'

/** Host half: browser presentation is exported through `./client`. */
export function apply(): void {}
