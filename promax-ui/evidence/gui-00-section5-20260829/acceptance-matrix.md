# Promax GUI：00 号文件第五节验收矩阵

时间：2026-08-29（浏览器隔离实例 `127.0.0.1:3184`）

| 规格 | 当前判定 | 真实实现 | 缺失 / 边界 | 假数据检查 |
|---|---|---|---|---|
| 5.1 / 5.1.1 常驻导航 | 通过 | 左栏常驻显示“新建草稿 / 草稿 / 产品智能体团队 / 项目组 / 设置”；团队页只替换主区域 | 无 | 无 |
| 5.2 草稿单向转团队 | 通过 | 三轮后出现转交入口；创建新的 r2 团队会话，不切换当前 preset | 无 | 无 |
| 5.3 项目组默认目录 | 通过 | 只填名称即可创建 `~/Promax/<项目>/`；高级入口可自定义父目录 | 无 | 无 |
| 5.4 项目目录规则 | 部分通过 | GUI/bundle 创建 `输入/草稿`、`输入/源文件`、`产出`、`.promax/drafts`、`.promax/judge` | 现役 r2 TeamRevision 的真实输出根仍是 `deliverables/`，与 00 号文件的 `产出/` 不一致；GUI 同时展示逻辑名与真实路径，不伪装一致 | 无 |
| 5.5 交底草案 | 通过 | 草稿会话右栏常驻、可收起；每轮增量维护，并写入 `$DSH_HOME/promax/drafts/state.json` | 当前增量提取是确定性规则，不调用模型；准确性仍需业务样本验收 | 无 |
| 5.6 转交交互 | 通过 | 三轮后常驻按钮；四段内容可编辑；写交底和原始对话；启动新团队会话 | 无 | 无 |
| 5.7 记录开关 | 通过 | 默认开；首次提示；关闭警告；中途开启二选一；补整理/压缩标记 | 无 | 无 |
| 5.8 三列团队界面 | 本轮通过可见结构 | ①常驻导航；②团队主页/原生对话；③常驻状态结果栏。顶部有可点击面包屑、文件抽屉、成员·7；进度树由 r2 文件责任生成，每个叶子有“生成/判定”两段状态 | 文件抽屉显示运行时定义的目录与产物清单，尚未接实时文件枚举；进度流仍是通用逐轮摘要，未做到 Judge 退回证据的完整对话流卡片 | 无；0 turn 明确显示“尚未开始 / 尚未生成 / 未判定”，没有把未运行 Judge 写成通过 |
| 5.9 决策弹窗 | 缺失 | 无 | 第 1 次退回通知、重做到顶三选一、worker 申诉裁决尚未接运行事件；本轮没有 Judge 运行数据，不能用静态演示冒充 | 未造假 |
| 5.10 草稿不产生正式文件 | GUI 通过 / 机械边界待核 | GUI 只对话与维护交底状态；正式产出入口只在团队 | 是否能从底层工具越过 GUI 写业务文件属于 preset / 工具策略，不在本轮 GUI 内证明 | 无 |
| 5.11 三项待定 | 未动 | 无 | 名称、折叠后进度行、多产物同时达重做上限的弹窗形态仍待负责人决定 | 无 |

## 5.1.1 四条浏览器证据

1. 任意屏常驻：最终团队页截图同时含“新建草稿”“产品智能体团队”和右侧“进度”。
2. DOM 不销毁：团队页记录左栏按钮节点 `新建草稿=node_id 2`、`产品智能体团队=node_id 4`；切到草稿后仍为 `node_id 2`、`node_id 4`，左栏其他节点 `1–9` 也全部保持。
3. 三次内回草稿：从团队项目页点击一次“新建草稿”即回到草稿会话；未刷新、未使用系统后退。
4. 面包屑逐级可点：团队会话点“产品”回项目首页；重新进入会话后点“产品智能体团队”回团队根“选择项目组”。

## 三列不遮挡的浏览器几何证据

- 左导航：`left=12, right=280, width=268`
- 主区域：`left=280, right=976, width=696`
- 右进度栏：`left=976, right=1280, width=304`
- `navMainOverlap=false`，`mainRailOverlap=false`

## 截图

- `team-progress-three-columns.png`
- `team-session-toolbar-r2.png`
- `team-files-judge-states.png`
- `team-members-r2.png`

## 发布边界

- 发布包：`@promax/promax-ui-console@0.3.6`
- 安装前后 `dsh --profile web --dump-config`：0 行 diff；没有 patch dsh-base 条目，也没有新增本轮以外的配置行。
- 单元/组件测试：56 项通过；typecheck、build、distribution 均通过。以上仅作回归证据，界面结论以本文件的浏览器点击为准。
